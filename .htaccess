RewriteEngine On
# Redirige toutes les requêtes sauf pour les fichiers ou dossiers existants vers index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
